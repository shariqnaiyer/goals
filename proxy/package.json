{
  "name": "goals-llm-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Thin, stateless LLM proxy for the Goals app (docs/PLAN.md §3.2).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
